<%= link_to 'Back to Lists', lists_path %>
<h1> <%= @list.name %> </h1>
<ul>
  <% @list.movies.each do |movie| %>
    <li >
      <%= @movies = Movie.all %>
      <%= movie.title %>
      <%= movie.overview %>
      <%= movie.rating %>
      <%= movie.poster_url %>
    </li>
  <% end %>
</ul>
